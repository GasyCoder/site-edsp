; Limites d'upload pour le serveur de dev (chargé via PHP_INI_SCAN_DIR, voir "composer run serve").
; Alignées sur MediaResource::MAX_UPLOAD_SIZE_KB (10 Mo) avec de la marge pour le corps de requête.
upload_max_filesize = 12M
post_max_size = 16M
