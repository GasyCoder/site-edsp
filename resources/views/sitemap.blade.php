{!! '<'.'?xml version="1.0" encoding="UTF-8"?'.'>' !!}<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>{{ route('home') }}</loc></url>@foreach($pages as $page)<url><loc>{{ route('pages.show',$page->slug) }}</loc><lastmod>{{ $page->updated_at->toAtomString() }}</lastmod></url>@endforeach @foreach($programs as $program)<url><loc>{{ route('programs.show',$program->slug) }}</loc></url>@endforeach @foreach($news as $article)<url><loc>{{ route('news.show',$article->slug) }}</loc></url>@endforeach</urlset>
