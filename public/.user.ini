; Limites d'upload en production (lu automatiquement par PHP-FPM/CGI).
; Alignées sur MediaResource::MAX_UPLOAD_SIZE_KB (10 Mo) avec marge pour le corps de requête.
upload_max_filesize = 12M
post_max_size = 16M
